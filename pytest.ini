[pytest]
testpaths = tests
addopts = --timeout=90
# A UI test that opens a modal dialog would otherwise wait forever for a click
# that never comes; failing after 90 seconds says so instead of hanging CI.
timeout_method = thread
