# Runtime dependencies.
# PySide6-Essentials is deliberate: the full PySide6 package pulls in Qt
# modules this app never touches and roughly doubles the download.
PySide6-Essentials>=6.7,<7
reportlab>=4.0
XlsxWriter>=3.1
